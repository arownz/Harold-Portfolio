// postcss.config.js is intentionally empty.
// Tailwind CSS v4 is processed via the @tailwindcss/vite Vite plugin (vite.config.ts).
// If you switch to a non-Vite bundler, replace with:
// export default { plugins: { '@tailwindcss/postcss': {} } }
export default {}
